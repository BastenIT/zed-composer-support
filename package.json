{
  "name": "zed-composer-support",
  "version": "0.1.0",
  "description": "Language server and tests for the Composer Support Zed extension",
  "private": true,
  "scripts": {
    "check": "node --check server/composer-language-server.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": "github:BastenIT/zed-composer-support"
}
